type Query {
  hello: String!
  user(id: ID!): User
  email: String
  token: String
  password: String
  name: String
  admin: Boolean
  id: ID
}

type User {
  id: ID!
  name: String!
  friends: [User!]!
}
